server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          # This points to a folder named 'config-repo' in your root workspace
          search-locations: classpath:/config-repo